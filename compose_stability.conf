// Types the Compose compiler cannot prove stable but that are immutable in practice.
// Only add a type here if you are willing to promise it never mutates observably.
// Verify with: ./gradlew :app:composeApp:compileKotlinJvm -PcomposeReports=true
kotlin.time.Instant
kotlin.time.Duration
kotlinx.datetime.LocalDate
kotlinx.datetime.LocalTime
kotlinx.datetime.LocalDateTime
kotlinx.datetime.TimeZone
